/**
 * Layout configuration for static site generation
 * Required for @sveltejs/adapter-static
 */

// Enable prerendering for all pages
export const prerender = true;

// Use client-side rendering (SPA mode)
export const ssr = false;
