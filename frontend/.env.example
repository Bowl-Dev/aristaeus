# Aristaeus Frontend Environment Variables
#
# This file is auto-generated by npm scripts. Do not edit directly.
# Instead, use one of the development modes:
#
#   npm run dev:local    - Uses .env.local (localhost:3000)
#   npm run dev:hybrid   - Uses .env.local (localhost:3000)
#   npm run dev:frontend - Uses .env.hosted (AWS API Gateway)
#
# Environment files:
#   .env.local          - Local backend URL (committed)
#   .env.hosted         - AWS API Gateway URL (gitignored, create from .env.hosted.example)
#   .env.hosted.example - Template for hosted config
#
# Manual setup:
#   cp frontend/.env.local frontend/.env     # For local backend
#   cp frontend/.env.hosted frontend/.env    # For hosted backend

VITE_API_URL=http://localhost:3000

# Base path for GitHub Pages deployment (only needed for production builds)
# BASE_PATH=/aristaeus
