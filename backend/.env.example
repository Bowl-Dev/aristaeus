# Aristaeus Backend Environment Variables
#
# This file is auto-generated by npm scripts. Do not edit directly.
# Instead, use one of the development modes:
#
#   npm run dev:local    - Uses .env.local (Docker PostgreSQL)
#   npm run dev:hybrid   - Uses .env.hosted (Aurora Serverless)
#   npm run dev:frontend - Backend not needed (uses hosted Lambda)
#
# Environment files:
#   .env.local          - Docker PostgreSQL (committed, safe)
#   .env.hosted         - Aurora credentials (gitignored, create from .env.hosted.example)
#   .env.hosted.example - Template for hosted config
#
# Manual setup:
#   cp backend/.env.local backend/.env        # For local development
#   cp backend/.env.hosted backend/.env       # For hosted database

DATABASE_URL="postgresql://aristaeus:aristaeus@localhost:5432/aristaeus?schema=public"
NODE_ENV=development
